# Tested on Python 3.14 (supported range 3.11 - 3.14).
# Versions below are the known-good baseline; newer patch releases are fine.

# UI
PySide6>=6.10.2

# Per-app volume / mute (pulls in comtypes)
pycaw>=20251023

# Windows Runtime projection for SMTC — keep these in lockstep with winrt-runtime
winrt-runtime>=3.2.1
winrt-Windows.Media>=3.2.1
winrt-Windows.Media.Control>=3.2.1
winrt-Windows.Storage.Streams>=3.2.1
winrt-Windows.Foundation>=3.2.1
winrt-Windows.Foundation.Collections>=3.2.1

# Optional: audio-spectrum visualizer. The app runs fine without these
# (the spectrum effect is simply disabled). numpy 2.4 requires Python >= 3.11.
numpy>=2.4.4
sounddevice>=0.5.5
